import {
  ADD_POST,
  EDIT_POST,
  UPVOTE_POST,
  DOWNVOTE_POST,
  DELETE_POST,
} from '../actions/post'

function post(state={}, action) {
  switch(action.type) {
    default:
      return state
  }
}

export default post